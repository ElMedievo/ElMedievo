<div class="container">
  <h1>» Anuncios Medievales</h1>
  <hr id="spacer"/>
  <% @posts.each do |post| %>
    <div class="well">
      <h3><%= post.title %></h3>
      <%= link_to "Leer", post_path(post), class: "btn btn-dark" %>
    </div>
    <hr/>
  <% end %>
</div>
