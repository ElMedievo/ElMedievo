<nav class='navbar navbar-default navbar-static-top'>
  <div class='container'>
    <div class='navbar-header'>
      <button class='navbar-toggle collapsed' data-target='#top-navbar-collapse' data-toggle='collapse' id='burger'>
        <span class='fa fa-bars'></span>
      </button>
      <a class='navbar-brand' href='/'>
        <%= image_tag "brand.jpg", :class => "pull-left", :height => "35px", :style => "border-radius: 3px;" %>
        ElMedievo
      </a>
    </div>
    <div class='navbar-collapse collapse' id='top-navbar-collapse'>
      <ul class='nav navbar-nav' style='margin-right: 0;'>
        <li>
          <a aria-expanded='false' aria-haspopup='true' href='<%= rules_path %>'>
            <i class="fa fa-book" aria-hidden="true"></i>
            Rules
          </a>
        </li>
        <li>
          <a aria-expanded='false' aria-haspopup='true' href='<%= history_path %>'>
            <i class="fa fa-map" aria-hidden="true"></i>
            History
          </a>
        </li>
        <!--
        <li class="dropdown">
          <a id="players-toggle" class="dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="fa fa-user" aria-hidden="true"></i>
            Players
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" aria-labelledby="access-toggle">
            <li>
              <a href="<%= staff_path %>">
                <i class="fa fa-user-md" aria-hidden="true"></i>
                Staff
              </a>
            </li>
            <li>
              <a href="<%= channels_path %>">
                <i class="fa fa-youtube-play" aria-hidden="true"></i>
                Channels
              </a>
            </li>
          </ul>
        </li>
        -->
      </ul>
      <ul class="nav navbar-nav pull-right">
        <li class="dropdown">
          <a id="access-toggle" class="dropdown-toggle" style="outline: none; <%= user_signed_in? ? 'line-height: 0px;' : '' %>" data-toggle="dropdown" href="#">
            <% if user_signed_in? %>
              <img class="profile-head-mini" src="<%= Minecraft::Data.head_url_of_uuid(current_user.uuid) %>" alt="">
            <% else %>
              <i class="fa fa-user"></i>
            <% end %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="access-toggle">
            <% if user_signed_in? %>
              <li>
                <%= link_to "Log out", destroy_user_session_path, :method => 'delete', :class => 'dropdown-item' %>
              </li>
            <% else %>
              <li>
                <%= link_to 'Login', new_user_session_path, :class => 'dropdown-item' %>
              </li>
              <!--
              <li>
                <%#= link_to 'Register', new_user_registration_path, :class => 'dropdown-item' %>
              </li>
              -->
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
